# from .setup import create_database, create_table, tables
from config import DB_HOST, DB_USER, DB_PASSWORD, DATABASE

# def create():
#     create_database( name=DATABASE)
#     for key, val in tables.items():
#         create_table(name=key, fields=val)

